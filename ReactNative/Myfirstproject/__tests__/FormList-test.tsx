import React from 'react'
import  Form from '../components/Form'
import {fireEvent, render} from '@testing-library/react-native'
 test(' form data', () => {
   const {getByText, getByPlaceholderText, getAllByText}=render(<Form/>);
   fireEvent.
 })
 
